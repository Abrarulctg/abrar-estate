
import { Helmet } from "react-helmet";

const UpdateProfile = () => {
    return (
        <div>
            <Helmet>
                <title>Abrar Estate | Update Profile</title>
                <meta name="description" content="Helmet application" />
            </Helmet>
            <h1 className="text-3xl">Update Profile</h1>
        </div>
    );
};

export default UpdateProfile;